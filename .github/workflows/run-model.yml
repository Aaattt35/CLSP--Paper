name: Run Optimization Model

on:
  push:
    branches:
      - main

jobs:
  run-model:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyomo highspy numpy pandas

      - name: Run experiment
        run: |
          python src/run_experiment.py instances/test_instance.txt

      - name: Upload single CSV artifact
        uses: actions/upload-artifact@v4
        with:
          name: test-instance-output
          path: results/test_instance_output.csv
